#include "main.h"
/**
 * print_rev - Prints a string in reverse.
 * @s: string to be reserved.
 */
void print_rev(char *s)
{
int lengh = 0, a;
while (s[a++])
len++;
for (a = len - 1; a >= 0; a--)
_putchar(s[a]);
_putchar('\n');
}
