#include "main.h"
/**
 * _isdigit - check the given value is a digit
 * @c: values to check
 * Return: Sucess tasks 1
 */
int _isdigit(int c)
{
if (c >= '0' && c <= '9')
{
return (1);
}
return (0);
}
