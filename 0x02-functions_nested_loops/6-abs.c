#include "main.h"
/**
 * _abs - return the absolute value of a given integer.
 * @a: value use to find the absolute value.
 * Return: Sucess taks 6.
 */
int _abs(int a)
{
if (a < 0)
return (-a);
return (a);
}
